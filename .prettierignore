# CODEMAP.md is generated by scripts/generate-codemap.js — formatting is owned
# by the generator, and reformatting via prettier would desynchronize the file
# from `npm run codemap` output and break byte-identical regeneration.
CODEMAP.md

# codemap.config.json is hand-edited. Pinned here only to keep its formatting
# stable across prettier-config changes (printWidth, JSON key indentation,
# etc.) so we don't get noise diffs whenever prettier defaults shift; the
# generator parses it via JSON.parse, which is agnostic to whitespace.
codemap.config.json
