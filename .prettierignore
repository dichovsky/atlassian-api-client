# CODEMAP.md is generated by scripts/generate-codemap.js — formatting is owned
# by the generator, and reformatting via prettier would desynchronize the file
# from `npm run codemap` output and break byte-identical regeneration.
CODEMAP.md

# codemap.config.json is hand-edited, not generated. It is pinned here so that
# adding fields later (e.g. nested arrays, comments-as-strings) does not get
# silently reformatted into a shape the generator's reader does not expect.
codemap.config.json
