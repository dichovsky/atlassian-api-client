# The generator always writes LF. `--check` mode normalizes CRLF→LF before
# comparing, so this attribute is NOT required for the staleness check to
# work. Its purpose is to prevent line-ending churn in `git diff` and PR
# review on Windows checkouts with core.autocrlf=true, which would otherwise
# convert LF→CRLF on checkout and produce noisy whole-file diffs the moment
# the file is regenerated.
CODEMAP.md text eol=lf
test/fixtures/codemap/CODEMAP.md text eol=lf
